export declare type ResolveUrlOpts = {
    protoPath: string;
    domainToMappings: DomainToMappings;
    data?: object;
    host: string;
};
export declare function resolveUrl(opts: ResolveUrlOpts): string;
declare type Mapping = {
    srcPath: string;
    destPath: string;
};
declare type DomainToMappings = Record<string, Mapping[]>;
export {};
