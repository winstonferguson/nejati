var _a;
import { ConverterType } from '../domain';
export var pageUrlV2 = (_a = {
        types: ['wix.common.PageUrlV2']
    },
    _a[ConverterType.FROM_JSON] = {
        transform: function (val) {
            if (!val) {
                return;
            }
            return val.url;
        },
    },
    _a[ConverterType.TO_JSON] = {
        transform: function (val) {
            if (!val) {
                return;
            }
            var pathname = new URL(val).pathname;
            return {
                relativePath: pathname,
                url: val, // (e.g https://mysite.com/product-page/a-product)
            };
        },
    },
    _a);
//# sourceMappingURL=page-url-v2.js.map