var _a;
import { ConverterType } from '../domain';
export var pageUrl = (_a = {
        types: ['wix.common.PageUrl']
    },
    _a[ConverterType.FROM_JSON] = {
        transform: function (val) {
            if (!val) {
                return;
            }
            return "http://".concat(val.base).concat(val.path);
        },
    },
    _a[ConverterType.TO_JSON] = {
        transform: function (val) {
            if (!val) {
                return;
            }
            var _a = new URL(val), host = _a.host, pathname = _a.pathname;
            return {
                base: host,
                path: pathname, // (e.g /product-page/a-product)
            };
        },
    },
    _a);
//# sourceMappingURL=page-url.js.map