var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var _a;
import { pick, omitBy, isUndefined } from 'lodash';
import { ConverterType } from '../domain';
var sharedFieldNames = ['city', 'subdivision', 'country', 'postalCode'];
var sharedStreetAddressFieldNames = ['name', 'number', 'apt'];
export var address = (_a = {
        types: ['wix.common.Address']
    },
    _a[ConverterType.FROM_JSON] = {
        /**
         * currently wix.common.Address represents the deserialized object, so we're good.
         * in case it contains some serializable stuff (e.g. int64), we'll have to make some
         * modifications in the type.
         */
        transform: function (val) {
            return (val &&
                omitBy(__assign({ formatted: val.formattedAddress, location: val.geocode, addressLine1: val.addressLine, addressLine2: val.addressLine2, streetAddress: val.streetAddress &&
                        pick(val.streetAddress, sharedStreetAddressFieldNames) }, pick(val, sharedFieldNames)), isUndefined));
        },
    },
    _a[ConverterType.TO_JSON] = {
        transform: function (val) {
            return (val &&
                omitBy(__assign(__assign({}, pick(val, sharedFieldNames)), { formattedAddress: val.formatted, geocode: val.location, addressLine: val.addressLine1, addressLine2: val.addressLine2, streetAddress: val.streetAddress &&
                        pick(val.streetAddress, sharedStreetAddressFieldNames) }), isUndefined));
        },
    },
    _a);
//# sourceMappingURL=address.js.map